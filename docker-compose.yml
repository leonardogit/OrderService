version: '3.9'

services:

  mysql:
    image: mysql:8.0

    container_name: order-mysql

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: orderdb

    ports:
      - "3306:3306"